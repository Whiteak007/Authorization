import express from "express";
import { ensureAuthenticated } from "../Middlewares/Auth.js";
const router = express.Router();

// The dummy products and the product route have been moved to the frontend.
// This file is now empty, ready for you to add new product-related API routes.

export default router;